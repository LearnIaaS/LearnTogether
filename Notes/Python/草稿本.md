# 草稿本

```python
#!/usr/bin/python

import json
import os
import configparser

cf = configparser.ConfigParser()
cf.read("/home/artist/Documents/test08/config.ini")

dic = cf.items("FIO")
print type(dic) # list
print type(dic[0])
print dic[0]

for item in dic:
    print item

filename =" --filename=" + cf.get("FIO", "filename")
runtime =" --runtime=" + cf.get("FIO", "runtime")

print(filename)
print(runtime)

```

